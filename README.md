# SFML-WhiteBoard-with-WebSockets

# Запуск

1. В основой папке написать команду:

```bash
cmake .
```

2. Перейти в создавшуюся папку `build`

3. Написать в этой папке команду:

```bash
make all
```

4. В папке build появится исполняемый файл `WhiteBoard`